name: 🚀 Deploy a FTP

on:
  push:
    branches:
      - main  # Cambia esto si quieres que se ejecute en otra rama

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Clonar el repositorio
        uses: actions/checkout@v4

      - name: 🔄 Subir archivos al FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: /dist/caliope-app/browser/  # Directorio local que se subirá (puedes cambiarlo)
          server-dir: /public_html/  # Directorio remoto en el FTP (ajústalo según tu estructura)
