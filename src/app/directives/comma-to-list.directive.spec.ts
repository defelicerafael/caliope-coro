import { CommaToListDirective } from './comma-to-list.directive';

describe('CommaToListDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaToListDirective();
    expect(directive).toBeTruthy();
  });
});
