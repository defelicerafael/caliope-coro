import { StickyOnScrollingDirective } from './sticky-on-scrolling.directive';

describe('StickyOnScrollingDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyOnScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
