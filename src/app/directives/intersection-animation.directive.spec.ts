import { IntersectionAnimationDirective } from './intersection-animation.directive';

describe('IntersectionAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
