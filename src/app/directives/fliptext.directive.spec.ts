import { FliptextDirective } from './fliptext.directive';

describe('FliptextDirective', () => {
  it('should create an instance', () => {
    const directive = new FliptextDirective();
    expect(directive).toBeTruthy();
  });
});
